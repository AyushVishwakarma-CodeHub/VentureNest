{
  "name": "startup-pitch-hub",
  "version": "1.0.0",
  "description": "Where Great Ideas Meet Great Investors - A SaaS platform for startup pitches, investments, and mentorship",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "start": "cd server && npm start",
    "lint": "concurrently \"cd server && npm run lint\" \"cd client && npm run lint\"",
    "seed": "cd server && npm run seed",
    "setup": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["startup", "pitch", "investment", "saas", "mern"],
  "license": "MIT"
}
